spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      host: localhost
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASSWORD}
      port: ${mongo-port}
      database: local