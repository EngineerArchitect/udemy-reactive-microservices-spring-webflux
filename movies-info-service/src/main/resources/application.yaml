spring:
  profiles:
    active: local
---
spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASSWORD}
      host: localhost
      port: 27017
      database: ${MONGO_DATABASE}
      authentication-database: admin
logging:
  level:
    root: debug

# Customizes the path where the raw JSON OpenAPI documentation is available (Default is /v3/api-docs)
#springdoc:
#  api-docs:
#    path: /api-docs
#  swagger-ui:
#    path: /swagger-ui.html

  # Customizes the path for the Swagger UI dashboard (Default is /swagger-ui/index.html)
---
spring:
  config:
    activate:
      on-profile: nonprod
  data:
    mongodb:
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASSWORD}
      host: localhost
      port: 27017
      database: ${MONGO_DATABASE}
      authentication-database: admin
---
spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      username: ${MONGO_USERNAME}
      password: ${MONGO_PASSWORD}
      host: localhost
      port: 27017
      database: ${MONGO_DATABASE}
      authentication-database: admin
---
logging:
  level:
    root: info